apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: prepare-to-perform-templates
  description: A collection of RHDH templates for "Prepare to Perform with Red Hat Developer Hub"
spec:
  type: url
  targets:
     - https://{{ gitlab_host }}/{{ gitlab_group }}/prepare-to-perform-templates/blob/master/nestjs-with-postgres/template.yaml
     - https://{{ gitlab_host }}/{{ gitlab_group }}/prepare-to-perform-templates/blob/master/quarkus-with-angular/template.yaml
     - https://{{ gitlab_host }}/{{ gitlab_group }}/prepare-to-perform-templates/blob/master/custom-component-patch/template.yaml
     - https://{{ gitlab_host }}/{{ gitlab_group }}/prepare-to-perform-templates/blob/master/custom-deployment-patch/template.yaml
